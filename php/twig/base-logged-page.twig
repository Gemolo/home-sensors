{% extends 'base-page.twig' %}
{% block precontent %}

    <nav class="nav nav-tabs">
        <a class="nav-link {% if page == 'home' %} active {% endif %}" href="{{ root }}">Sensori</a>
        <a class="nav-link {% if page == 'settings' %} active {% endif %}" href="{{ root }}/settings/users">Settings</a>
        <a class="nav-link {% if page == 'logout' %} active {% endif %}" href="{{ root }}/logout" style="margin-left: auto">Logout</a>

    </nav>
    <style>
        .content{
            margin: 32px;
        }

        nav {
            margin: 8px;
        }
    </style>
{% endblock %}